import Control from '@/type/Control'

type Stratagem = {
    image: string,
    name: string,
    code: Control[],
    cooldown: string,
    uses: string,
    activation: string,
    lookup: string,
    category: string
}

export default Stratagem